
          // IMPORTANTE: ExcluÃ­mos a data de checkout da verificaÃ§Ã£o, pois Ã© permitido fazer
          // checkout em um dia que estÃ¡ indisponÃ­vel (o checkout Ã© pela manhÃ£)
>         const temDatasIndisponiveisNoIntervalo = datasIndisponiveis.some(dataIndisponivel => {
            return (
              isAfter(dataIndisponivel, range.from as Date) && 
              isBefore(dataIndisponivel, range.to as Date) &&
              !isSameDay(dataIndisponivel, range.to as Date) // Excluir dia de checkout da verificaÃ§Ã£o
            );
          });
          
>         if (temDatasIndisponiveisNoIntervalo) {
            toast({
              title: "PerÃ­odo indisponÃ­vel",
              description: "Existem datas bloqueadas no perÃodo selecionado. Por favor, escolha outro perÃ-
odo.",
              variant: "destructive"
            });
            return;
          }
        }


